<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="styles/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>AccWeather</title>
</head>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #abd4ff;
        }
        
        .container-fluid {
            padding-left: 0;
            padding-right: 0;
            background-color: rgb(5, 71, 122);
        }
        
        header {
            padding: 2rem 1rem; /* Adjust padding for header size */
        }
        
        header .fs-1 {
            font-size: 2.5rem; /* Increase the font size */
            font-weight: bold;
            color: yellowgreen;
        }
        
        .nav-pills .nav-link {
            font-size: 1.25rem; /* Increase font size of links */
            padding: 0.75rem 1.25rem; /* Increase padding for larger clickable area */
            color: yellow; 
        }
        
        .nav-pills .nav-link.active {
            font-size: 1.25rem; /* Ensure active link has the same size */
            background-color: rgb(5, 71, 122); /* Yellowish-green color for the active link */
            color: white; /* Optional: Set the text color to white for better contrast */
        }
        
        .nav-item {
            margin-right: 1rem; /* Add some spacing between nav items */
        }
        
        svg {
            width: 48px; /* Increase the width of the SVG icon */
            height: 48px; /* Increase the height of the SVG icon */
        }
        
        .nav-item .nav-link svg {
            width: 24px; /* Set to desired width */
            height: 28px; /* Set to desired height */
        }
        
        .card {
            border: none;
        }
        
        .card-header {
            background-color: rgb(5, 71, 122);
            color: white;
            font-weight: bold;
        }
        
        .btn-primary {
            background-color: rgb(5, 71, 122);
            border: none;
        }
        
        .btn-primary:hover {
            background-color: rgb(5, 71, 122);
        }
        
        .form-label {
            font-weight: bold;
        }
        
        .container {
            margin-top: 50px;
        }
        /* Additional styles specific to this page */
        body {
            background-color: #abd4ff;
        }

        /* Ensure footer styles are present here */
        .footer {
            background-color: rgb(5, 71, 122);
            color: white;
            padding: 2rem 0;
        }

        .footer a {
            color: #fff;
            text-decoration: none;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        .footer .social-icons a {
            margin-right: 1rem;
        }
        
    </style>